# Copy this file to .env and adjust values before running.

SITE_NAME=press.localhost
ADMIN_PASSWORD=admin
DB_ROOT_PASSWORD=change-this
APP_PORT=9500
# Set to the public URL used to access this site (e.g. https://cloud.example.com)
# Required when accessed via a reverse proxy with a different hostname than SITE_NAME
HOST_NAME=
